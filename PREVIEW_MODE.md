# Docker 手動控制模式 - 使用指南

## ✅ 配置說明

Docker 容器配置為**保持運行但不自動啟動服務**。您需要進入容器手動啟動服務。

**注意**: 由於移除了 Port 4000 的配置，Dev 和 Preview 模式都將使用 **Port 3000**。這意味著同一時間只能運行其中一種模式。

## 🚀 使用步驟

### 1. 啟動容器

```bash
docker-compose down
docker-compose up -d
```

### 2. 進入容器

```bash
docker exec -it nuxt-frontend sh
```

### 3. 選擇模式（二選一）

#### 選項 A: 開發模式 (Dev)
```bash
npm run dev
```
- **網址**: http://localhost:3000
- **用途**: 日常開發、熱重載

#### 選項 B: 預覽模式 (Preview)
```bash
npm run build
npm run preview
```
- **網址**: http://localhost:3000
- **用途**: 測試生產構建、Lighthouse 測試
- **注意**: 運行前請確保 Dev 模式已停止

## 🔄 切換模式流程

1. 在容器終端中按 `Ctrl+C` 停止當前服務
2. 輸入新命令啟動另一模式
   - 例如從 Dev 切換到 Preview: `npm run build && npm run preview`

## 故障排除

**Q: 容器一直重啟？**
A: 請確認 `docker-compose.yml` 中的 `command` 是 `tail -f /dev/null`。

**Q: 無法訪問 localhost:3000？**
A: 請確認您已經在容器內手動執行了 `npm run dev` 或 `npm run preview`。
